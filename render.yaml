
services:
  - type: web
    name: healthapi
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn healthapi.wsgi:application"
    dockerfilePath: ./Dockerfile  # Remove this line if not using Docker
